import React from 'react';
import { AppBar, Button, IconButton, Avatar, Toolbar, Drawer } from '@mui/material';
import { Menu, AccountCirlce, Brightness4, Brightness7 } from '@mui/icons-material';
import { Link } from 'react-router-dom';

const NavBar = () => {
  return (
    <div>NavBar</div>
  );
};

export default NavBar;
